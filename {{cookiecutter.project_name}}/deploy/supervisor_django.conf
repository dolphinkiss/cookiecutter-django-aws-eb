[program:djang_app_{{ cookiecutter.project_name }}]
command=/app/{{ cookiecutter.project_name }}/venv/bin/uwsgi
    --chdir /app/{{ cookiecutter.project_name }}
    --env DJANGO_SETTINGS_MODULE={{ cookiecutter.project_name }}.settings_production
    --env PYTHONHASHSEED=random
    --env LANG=en_US.UTF-8
    --socket /app/{{ cookiecutter.project_name }}/uwsgi.sock
    --chmod-socket
    --module {{ cookiecutter.project_name }}.wsgi:application
    --master
    --processes 10
    --no-orphans
    --vacuum
    --log-4xx
    --log-5xx
    --single-interpreter
    --log-zero
    --log-slow 1000
    --virtualenv /app/{{ cookiecutter.project_name }}/venv
    --logdate
environment=PYTHONHASHSEED="random"
directory=/app/{{ cookiecutter.project_name }}
numprocs=1
stdout_logfile=/var/log/django_app_{{ cookiecutter.project_name }}.log
autostart=true
autorestart=true
startsecs=10
startretries=5
redirect_stderr=true
stopsignal=QUIT
stopwaitsecs=60

[program:nginx-app]
command = /usr/sbin/nginx
